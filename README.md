# OpenALEX Collector

OpenALEX APIを活用した学術論文収集・可視化ツール。
単一HTMLファイルで動作し、サーバー不要でブラウザだけで利用できます。

![Powered by OpenAlex API](https://img.shields.io/badge/Powered%20by-OpenAlex%20API-1a73e8)
![License](https://img.shields.io/badge/License-MIT-green)

---

## 概要

**OpenALEX Collector** は、[OpenAlex API](https://openalex.org/) を使って学術論文を検索・収集し、時系列推移や著者・機関ランキングを可視化するWebツールです。

- **単一HTMLファイル** — `OpenALEX_Collector.html` をブラウザで開くだけ
- **サーバー不要** — 完全クライアントサイド動作
- **共有が簡単** — ファイル1つを送るだけで誰でも利用可能

## 機能一覧

### 論文検索

| 機能 | 説明 |
|------|------|
| キーワード検索 | スペース区切り = AND / 引用符 = フレーズ一致 |
| 複数クエリOR検索 | 1行1クエリ、複数行で自動OR統合（重複除去） |
| 所属機関フィルタ | 機関名でAND絞り込み。セミコロン区切りで複数機関OR指定可 |
| 年範囲フィルタ | 開始年〜終了年で絞り込み |
| 年別取得モード | 出版年ごとに検索し、10,000件/クエリ制限を回避 |
| 論文種別フィルタ | Article, Review, Book Chapter, Preprint 等 |
| ソート順 | 関連度順 / 引用数順 / 新しい順 / 古い順 |
| 最小引用数フィルタ | 指定引用数以上の論文のみ取得 |
| オープンアクセスフィルタ | OA論文のみに絞り込み |
| キャンセル機能 | 検索中にAbortController経由で中断可能 |

### データ表示

- ソート可能なデータテーブル（タイトル / 年 / 著者 / 所属機関 / ジャーナル / 引用数 / DOI）
- ページネーション（50件/ページ）
- DOIリンク付き

### グラフ可視化

| グラフ | 説明 |
|--------|------|
| 件数推移（棒グラフ） | 年別論文件数 + CAGR・トレンド表示 |
| 件数推移（折れ線） | 全体推移 / 著者比較モード（最大5名選択） |
| 著者ランキング | 横棒グラフ、表示数指定可（デフォルト20名） |
| 機関ランキング | 横棒グラフ、表示数指定可（デフォルト20機関） |

### エクスポート

- **CSV** — BOM付きUTF-8（Excel日本語対応）
- **PNG** — 各グラフを画像としてダウンロード
- **CSVアップロード** — 以前保存したCSVを読み込み、再検索不要で可視化

## 使い方

### 1. ファイルを開く

`OpenALEX_Collector.html` をブラウザ（Chrome / Edge / Firefox / Safari）で開きます。

### 2. 検索を実行

1. **検索キーワード**を入力（複数行でOR検索）
2. 必要に応じて**所属機関フィルタ**を入力（例: `Harvard University; MIT`）
3. **年範囲**と**取得上限**を設定
4. 「検索を実行」をクリック

### 3. 結果を確認・可視化

- メトリクス（取得件数 / 要約あり件数 / CAGR / トレンド）を確認
- データテーブルでソート・閲覧
- グラフタブで可視化（棒グラフ / 折れ線 / 著者ランキング / 機関ランキング）

### 4. エクスポート

- 「CSVダウンロード」でデータを保存
- 「PNGダウンロード」でグラフを画像保存

## 検索構文

```
キーワード検索:     solid state battery          → AND検索
フレーズ一致:       "solid state battery"         → 完全一致
OR検索:            1行目: "solid state battery"   → 各クエリを個別検索し
                   2行目: "all solid battery"       重複除去して統合
機関フィルタ:       Toyota; Samsung SDI           → セミコロン区切りでOR
```

## 技術仕様

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML / CSS / JavaScript（単一ファイル） |
| グラフ描画 | [Chart.js v4](https://www.chartjs.org/)（CDN） |
| API | [OpenAlex REST API](https://docs.openalex.org/) |
| ページネーション | Cursor-based（10,000件制限なし） |
| レート制限 | 150ms間隔（polite pool: 10 req/sec） |
| リトライ | 最大5回、429エラー時は指数バックオフ |
| 文字コード | UTF-8（CSV出力はBOM付き） |

## 注意事項

- OpenAlex APIは認証不要ですが、大量取得時はレート制限にご注意ください
- 所属機関フィルタは入力名をOpenAlex Institutions APIで自動解決するため、部分一致の場合は意図しない機関にマッチすることがあります。正確なマッチには正式名称の入力を推奨します
- 年別取得モードは年数 × ページネーション回数分のAPIコールが発生するため、広い年範囲での大量取得には時間がかかります

## ライセンス

MIT License

---

&copy;2026 しばやま
